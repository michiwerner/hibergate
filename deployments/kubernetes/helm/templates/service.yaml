apiVersion: v1
kind: Service
metadata:
  name: {{ include "hibergate.fullname" . }}
  labels:
    {{- include "hibergate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hibergate.selectorLabels" . | nindent 4 }}
